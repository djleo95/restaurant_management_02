<section id="gallery" class="parallax-section">
  <div class="container">
    <div class="row">
      <div class="col-md-offset-2 col-md-8 col-sm-12 text-center">
        <h1 class="heading"><%= t "menu.menu"%></h1>
        <div class="apple_pagination">
          <%= will_paginate @dishes, container: false %>
        </div>
      </div>
      <%= render @dishes %>
    </div>
  </div>
</section>
